import React from "react";
import "./Header.css"

const MessageList = (props) => {
    

    return(
        <header className="header">
            <h1>Dashboard</h1>
        </header>
    );
};

export default MessageList;